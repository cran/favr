% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/abortifnot.r
\name{abortifnot}
\alias{abortifnot}
\alias{abortif}
\title{Ensure the truth of R expressions}
\usage{
abortifnot(
  ...,
  message = NULL,
  call = .envir,
  .envir = parent.frame(),
  .frame = .envir,
  abort_args = NULL
)

abortif(
  ...,
  message = NULL,
  call = .envir,
  .envir = parent.frame(),
  .frame = .envir,
  abort_args = NULL
)
}
\arguments{
\item{...}{Any number of R expressions, which should each evaluate to
(a \code{\link{logical}} vector of \strong{all}) \code{\link{TRUE}} for no error to occur (\code{FALSE}
for \code{abortif()}). Non-\code{logical} and \code{NA} values will trigger an error.

If an expression is named, the name will be used in the error message
instead of the default message or the \code{message} argument.}

\item{message}{Default error message for non-named expressions.}

\item{call}{An execution environment, defused function call, or \code{NULL}.
Passed to \code{\link[cli:cli_abort]{cli_abort()}}.}

\item{.envir}{Environment to evaluate the cli formatting of the error
message in. Passed to \code{\link[cli:cli_abort]{cli_abort()}}.}

\item{.frame}{The throwing context. Passed to
\code{\link[cli:cli_abort]{cli_abort()}}.}

\item{abort_args}{A list of additional arguments to pass to
\code{\link[rlang:abort]{abort()}} (forwarded from \code{\link[cli:cli_abort]{cli_abort()}}).}
}
\value{
\code{NULL} invisibly if the checks pass, otherwise an error is thrown.
}
\description{
If any of the expressions in \code{...} are not (\code{\link{all}}) \code{TRUE},
\code{\link[cli:cli_abort]{cli_abort()}} is called, producing an error
message indicating the first expression which was not (\code{\link{all}})
\code{TRUE}.

For \code{abortif()}, the opposite is true, i.e. expressions
should evaluate to (\code{\link{all}}) \code{FALSE} for no error to occur.
}
\examples{
abortifnot(1 == 1, all.equal(pi, 3.14159265), 1 < 2) # all TRUE

m <- matrix(c(1, 3, 3, 1), 2, 2)
abortifnot(m == t(m), diag(m) == rep(1, 2)) # all TRUE

abortifnot(1) |> try()

# A custom error message can be given for each expression:
m[1, 2] <- 12
abortifnot("{.var m} must be {.cls symmetric}" = m == t(m)) |>
  try()

# Alternatively, one error message can be used for all
# expressions.
abortifnot(
  m[1, 1] == 1,
  diag(m) == rep(2, 2),
  message = "{.var m} has a diagonal of: {diag(m)}"
) |> try()

# The `call` argument can be used to specify where the
# error occurs, by default this is the caller environment.
myfunc <- function(x) abortifnot(x)
myfunc(FALSE) |> try()

# abortif() errors if any argument does not evaluate to
# (all) FALSE.
abortif(c(T, F)) |> try()
abortif(c(T, NA)) |> try()
}
\seealso{
\code{\link[=stopifnot]{stopifnot()}} for the base \strong{R} function this is based on.

\code{\link[=check]{check()}} and \code{\link[=check_with]{check_with()}} for a non data-masked
and data-masked version of \code{abortifnot()} with tidy evaluation and
\link[rlang:topic-inject]{injection} support.
}
