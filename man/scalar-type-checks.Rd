% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar_type_checks.r
\name{scalar-type-checks}
\alias{scalar-type-checks}
\alias{check_scalar_list}
\alias{check_scalar_atomic}
\alias{check_scalar_vector}
\alias{check_scalar_integer}
\alias{check_scalar_integerish}
\alias{check_scalar_double}
\alias{check_scalar_complex}
\alias{check_scalar_character}
\alias{check_scalar_logical}
\alias{check_scalar_raw}
\alias{check_scalar_bytes}
\alias{check_scalar_numeric}
\title{Scalar type checks}
\usage{
check_scalar_list(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_atomic(
  x,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_vector(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_integer(
  x,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_integerish(
  x,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_double(
  x,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_complex(
  x,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_character(
  x,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_logical(
  x,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_raw(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_bytes(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_scalar_numeric(
  x,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)
}
\arguments{
\item{x}{An object to check.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{allow_na}{Whether \code{x} is allowed to
contain \code{NA} values.}

\item{finite}{Whether \code{x} is required to contain only finite values
(i.e. no \code{NA}, \code{Inf}, \code{-Inf}, or \code{NaN}).}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check if inputs are scalars of an expected type and
throw an error if not.
}
\details{
These functions can be used with the \code{\link[=bare]{bare()}} modifier to check if an
object is a bare R object (i.e. has no class attribute).
}
\note{
To handle empty strings (\code{""}) use \code{\link[=check_string]{check_string()}} instead of
\code{check_scalar_character()}.

These check functions are wrappers of their corresponding
\link[rlang:scalar-type-predicates]{rlang} functions. The exception
is \code{check_scalar_numeric()}, which uses \code{\link[=is.numeric]{is.numeric()}}.
}
\examples{
x <- 1L
check_scalar_integer(x)
check_scalar_double(x) |> try()

check_scalar_list(list(list()))
check_scalar_list(list(1, 2)) |> try()

check_scalar_character(NA_character_, allow_na = FALSE) |> try()
check_scalar_double(Inf, finite = TRUE) |> try()

check_scalar_logical(NULL, allow_null = TRUE)

x <- 1.0
check_scalar_integerish(x)
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{forbidden-value-checks}},
\code{\link{inheritance-checks}},
\code{\link{oop-checks}},
\code{\link{path-checks}},
\code{\link{property-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-value-checks}},
\code{\link{type-checks}},
\code{\link{walk-check}}
}
\concept{checks}
