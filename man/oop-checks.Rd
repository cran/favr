% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oop_checks.r
\name{oop-checks}
\alias{oop-checks}
\alias{check_s3}
\alias{check_s4}
\alias{check_s7}
\alias{check_r6}
\title{Check if an object is of a specific object-oriented programming type}
\usage{
check_s3(x, ..., allow_null = FALSE, arg = caller_arg(x), call = caller_env())

check_s4(x, ..., allow_null = FALSE, arg = caller_arg(x), call = caller_env())

check_s7(x, ..., allow_null = FALSE, arg = caller_arg(x), call = caller_env())

check_r6(x, ..., allow_null = FALSE, arg = caller_arg(x), call = caller_env())
}
\arguments{
\item{x}{An object to check.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check that an object is an \code{S3}, \code{S4}, \code{S7}, or \code{R6} object.
}
\details{
\code{S3} checks are performed by checking if the object has a class
attribute with \code{\link[=is.object]{is.object()}} and is not an \code{S4} object.

\code{S4} checks are performed using \code{\link[=isS4]{isS4()}}.

\code{S7} and \code{R6} checks are performed by checking for the inheritance of
the \code{S7_object} and \code{R6} classes, respectively.
}
\examples{
check_s3(factor("a"))
check_s3(1:3) |> try()

methods::setClass("Person",
  slots = c(name = "character", age = "numeric")
)
x <- methods::new("Person", name = "John", age = 30)

check_s4(x)
check_s4(factor("a")) |> try()

# trivial examples of inheritance checks for S7 and R6 objects
x <- structure(list(), class = "S7_object")
check_s7(x)
check_s7(factor("a")) |> try()

x <- structure(list(), class = "R6")
check_r6(x)
check_r6(factor("a")) |> try()
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{forbidden-value-checks}},
\code{\link{inheritance-checks}},
\code{\link{path-checks}},
\code{\link{property-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-type-checks}},
\code{\link{scalar-value-checks}},
\code{\link{type-checks}},
\code{\link{walk-check}}
}
\concept{checks}
