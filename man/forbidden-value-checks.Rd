% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forbidden_value_checks.r
\name{forbidden-value-checks}
\alias{forbidden-value-checks}
\alias{check_no_na}
\alias{check_finite}
\alias{check_unique}
\alias{check_nzchar}
\title{Forbidden value checks}
\usage{
check_no_na(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_finite(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_unique(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_nzchar(
  x,
  ...,
  allow_all_ws = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)
}
\arguments{
\item{x}{An object to check.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{allow_all_ws}{Whether \code{x} is allowed to contain elements that are
all whitespace.}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check if inputs contain forbidden values and error if so.
}
\details{
\code{NA} checks are done with \code{\link[=anyNA]{anyNA()}};

finite checks are done with \code{\link[=any]{any()}} and \code{\link[=is.finite]{is.finite()}};

unique checks are done with \code{\link[=anyDuplicated]{anyDuplicated()}};

zero chr checks are done with \code{\link[=any]{any()}} and \code{\link[=nzchar]{nzchar()}};

If \code{allow_all_ws = FALSE} then whitespace elements are identified using
\code{\link[=grepl]{grepl("\\\\s+", x)}}.

Input types are not checked, they are passed 'as is' to the functions
that do the forbidden value checking. The only exception is for \code{NULL}
inputs, which error if \code{allow_null = FALSE}.
}
\note{
\code{NA_character_} is not considered zero chr nor all whitespace.
}
\examples{
x <- c(1, 2, NA)
check_no_na(x) |> try()

x <- c(1, 2, Inf)
check_finite(x) |> try()

x <- c(1, 2, 3, 1)
check_unique(x) |> try()

x <- c("a", "b", "")
check_nzchar(x) |> try()

x <- c("a", "b", " ")
check_nzchar(x, allow_all_ws = FALSE) |> try()
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{inheritance-checks}},
\code{\link{oop-checks}},
\code{\link{path-checks}},
\code{\link{property-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-type-checks}},
\code{\link{scalar-value-checks}},
\code{\link{type-checks}},
\code{\link{walk-check}}
}
\concept{checks}
