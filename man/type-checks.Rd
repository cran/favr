% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type_checks.r
\name{type-checks}
\alias{type-checks}
\alias{check_list}
\alias{check_atomic}
\alias{check_vector}
\alias{check_integer}
\alias{check_integerish}
\alias{check_double}
\alias{check_complex}
\alias{check_character}
\alias{check_logical}
\alias{check_raw}
\alias{check_bytes}
\alias{check_null}
\alias{check_numeric}
\title{Type checks}
\usage{
check_list(
  x,
  n = NULL,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_atomic(
  x,
  n = NULL,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_vector(
  x,
  n = NULL,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_integer(
  x,
  n = NULL,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_integerish(
  x,
  n = NULL,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_double(
  x,
  n = NULL,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_complex(
  x,
  n = NULL,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_character(
  x,
  n = NULL,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_logical(
  x,
  n = NULL,
  ...,
  allow_na = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_raw(
  x,
  n = NULL,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_bytes(
  x,
  n = NULL,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_null(x, ..., arg = caller_arg(x), call = caller_env())

check_numeric(
  x,
  n = NULL,
  ...,
  finite = FALSE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)
}
\arguments{
\item{x}{An object to check.}

\item{n}{The expected length of \code{x}.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{allow_na}{Whether \code{x} is allowed to contain \code{NA} values.}

\item{finite}{Whether \code{x} is required to contain only finite values
(i.e. no \code{NA}, \code{Inf}, \code{-Inf}, or \code{NaN}).}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check if inputs are expected types and throw an error if not.
}
\details{
These functions can be used with the \code{\link[=bare]{bare()}} modifier to check if
an object is a bare R object (i.e. has no class attribute), and the
length modifiers \code{\link[=at_least]{at_least()}}, \code{\link[=at_most]{at_most()}}, and \code{\link[=in_range]{in_range()}} to
modify the behaviour of the length checking \code{n} argument.
}
\note{
\code{check_null()} cannot use \code{bare()} since \code{NULL} cannot have a class
attribute.

These check functions are wrappers of their corresponding
\link[rlang:type-predicates]{rlang} functions. The exception is
\code{check_numeric()}, which uses \code{\link[=is.numeric]{is.numeric()}}.
}
\examples{
x <- c(1, 2, 3)

check_integer(x) |> try()
check_integerish(x)
check_scalar_double(x) |> try()
check_double(x, n = 2) |> try()
check_double(x, n = at_least(4)) |> try()
check_double(x, n = at_most(2)) |> try()
check_double(x, n = in_range(1, 2)) |> try()

check_integer(bare(factor(1))) |> try()

check_double(c(1L, NA), allow_na = FALSE) |> try()
check_double(c(1.5, NA), finite = TRUE) |> try()

check_double(NULL, allow_null = TRUE)

# NULL list elements are not considered NULL
check_list(list(NULL), allow_null = TRUE) |> try()
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{forbidden-value-checks}},
\code{\link{inheritance-checks}},
\code{\link{oop-checks}},
\code{\link{path-checks}},
\code{\link{property-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-type-checks}},
\code{\link{scalar-value-checks}},
\code{\link{walk-check}}
}
\concept{checks}
