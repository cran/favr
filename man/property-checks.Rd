% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/property_checks.r
\name{property-checks}
\alias{property-checks}
\alias{check_length}
\alias{check_nrow}
\alias{check_ncol}
\alias{check_size}
\alias{check_non_empty}
\alias{check_named}
\title{Object property checks}
\usage{
check_length(
  x,
  n,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_nrow(
  x,
  nrow,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_ncol(
  x,
  ncol,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_size(
  x,
  n,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_non_empty(x, ..., arg = caller_arg(x), call = caller_env())

check_named(
  x,
  ...,
  unique = FALSE,
  allow_empty = TRUE,
  arg = caller_arg(x),
  call = caller_env()
)
}
\arguments{
\item{x}{An object to check.}

\item{n, nrow, ncol}{The expected length/size, number of columns, or number of
rows of \code{x}.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{unique}{Whether \code{x} is required to have unique names.}

\item{allow_empty}{Whether \code{x} is allowed to have empty names (\code{""}).}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check if inputs have certain properties and error if not.
}
\details{
\code{check_size()} uses \code{\link[vctrs:vec_size]{vec_size()}} to determine the size
of \code{x}, as opposed to \code{\link[base:length]{length()}} which is used by
\code{check_length()}.

Input types are not checked, they are passed 'as is' to the functions
that do the property checking. The only exception is for \code{NULL} inputs,
which error if \code{allow_null = FALSE}.

\code{check_length()}, \code{check_size()}, \code{check_nrow()} and \code{check_ncol()} can
be used with the length modifiers \code{\link[=at_least]{at_least()}}, \code{\link[=at_most]{at_most()}}, and
\code{\link[=in_range]{in_range()}} to modify the behaviour of the length checking \code{n}, \code{nrow},
or \code{ncol} arguments.
}
\examples{
x <- c(1, 2, NA)
check_length(x, 4) |> try()
check_size(x, 4) |> try()

# length modifiers can be used
check_length(x, at_most(2)) |> try()

x <- data.frame(x = 1)
check_nrow(x, 2) |> try()
check_ncol(x, in_range(2, 4)) |> try()
check_size(x, at_least(2)) |> try()

x <- numeric(0)
check_non_empty(x) |> try()
check_non_empty(NULL) |> try()

x <- c(1, 2, 3)
check_named(x) |> try()
names(x) <- c("a", "b", "a")
check_named(x, unique = TRUE) |> try()
names(x) <- c("a", "b", "")
check_named(x, allow_empty = FALSE) |> try()

check_length(NULL, 2, allow_null = TRUE)
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{forbidden-value-checks}},
\code{\link{inheritance-checks}},
\code{\link{oop-checks}},
\code{\link{path-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-type-checks}},
\code{\link{scalar-value-checks}},
\code{\link{type-checks}},
\code{\link{walk-check}}
}
\concept{checks}
