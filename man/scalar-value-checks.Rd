% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar_value_checks.r
\name{scalar-value-checks}
\alias{scalar-value-checks}
\alias{check_true}
\alias{check_false}
\alias{check_bool}
\alias{check_string}
\title{Scalar value checks}
\usage{
check_true(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_false(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_bool(
  x,
  ...,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)

check_string(
  x,
  ...,
  string = NULL,
  allow_empty = TRUE,
  allow_null = FALSE,
  arg = caller_arg(x),
  call = caller_env()
)
}
\arguments{
\item{x}{An object to check.}

\item{...}{Additional arguments passed to \code{\link[cli:cli_abort]{cli_abort()}}
which forwards unmatched arguments to \code{\link[rlang:abort]{abort()}}.}

\item{allow_null}{Whether \code{x} is allowed to be \code{NULL}.}

\item{arg}{An argument name as a string. This argument
will be mentioned in error messages as the input that is at the
origin of a problem.}

\item{call}{The execution environment of a currently
running function, e.g. \code{caller_env()}. The function will be
mentioned in error messages as the source of the error. See the
\code{call} argument of \code{\link[rlang:abort]{abort()}} for more information.}

\item{string}{A character vector of allowed values for \code{x}. If \code{NULL},
the value is not checked. The check passes if \code{x} is \strong{any} of the
values in \code{string}.}

\item{allow_empty}{Whether \code{x} is allowed to be an empty string
(i.e. when \code{FALSE}, \code{""} is not allowed).}
}
\value{
\code{NULL} invisibly if the check passes, otherwise an error is thrown.
}
\description{
Check if inputs are expected scalar values and
throw an error if not.
}
\examples{
x <- TRUE
check_true(x)
check_false(x) |> try()

check_bool(NA) |> try()
check_bool(NULL, allow_null = TRUE)

x <- "a"
check_string(x)
check_string(x, string = c("a", "b"))
check_string(x, string = c("b", "c")) |> try()
check_string("", allow_empty = FALSE) |> try()
}
\seealso{
Other checks:
\code{\link{array-type-checks}},
\code{\link{forbidden-value-checks}},
\code{\link{inheritance-checks}},
\code{\link{oop-checks}},
\code{\link{path-checks}},
\code{\link{property-checks}},
\code{\link{s3-type-checks}},
\code{\link{scalar-type-checks}},
\code{\link{type-checks}},
\code{\link{walk-check}}
}
\concept{checks}
